@model IEnumerable<CMS.MODEL.Master.CourseTeacherModel>

@{
    ViewBag.Title = "CollegeEnrolledTeachers";
    Layout = null;
}

<section class="section">
    <div class="container">
        <!-- course list -->
        <div class="row justify-content-center">
            <!-- course item -->
            @foreach (var item in Model)
            {
                <div class="col-lg-4 col-sm-6 mb-5">
                    <div class="card p-0 border-primary rounded-0 hover-shadow" @*data-aos="flip-left" data-aos-easing="ease-out-cubic" data-aos-duration="1000"*@>
                        <img class="card-img-top rounded-0" src="@Url.Content(@item.ImagePath)" alt="Teacher thumb">
                        <div class="card-body">
                            <ul class="list-inline mb-2">
                                <li class="list-inline-item" @*data-aos="fade-right"*@>@item.RoleName</li>
                            </ul>
                            <a href="course-single.html">
                                <h4 class="card-title" @*data-aos="fade-right" data-aos-delay="250"*@>@Html.DisplayFor(model => item.TeacherName)</h4>
                            </a>
                            <a href="/AdminPortal/TeacherCourseDetails?TID=@item.TeacherGuid" target="_blank" class="btn btn-primary btn-sm" @*data-aos="fade-up" data-aos-delay="300"*@>View Details</a>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</section>


